<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- =================================================================== -->
	<!-- SIMPLIFIED ENTITLEMENTS FOR INITIAL TESTFLIGHT BUILD -->
	<!-- =================================================================== -->
	<!-- Only essential capabilities that work with automatic signing -->
	<!-- Advanced capabilities (App Groups, iCloud, HomeKit) can be added -->
	<!-- after provisioning profiles are properly configured -->
	<!-- =================================================================== -->

	<!-- HealthKit Access (basic - no health-records which requires approval) -->
	<key>com.apple.developer.healthkit</key>
	<true/>

	<!-- Audio Background Mode (for audio playback) -->
	<!-- Note: inter-app-audio is deprecated, using UIBackgroundModes in Info.plist instead -->

</dict>
</plist>
