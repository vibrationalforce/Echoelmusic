// User Routes (placeholder for future user management)
import { Router } from 'express';

const router = Router();

// Future user management endpoints will go here
// For now, user profile is handled in auth routes

export default router;
